context("GitHub", () => {
  beforeEach(() => {});

  it("should work", () => {
    cy.visit("https://developer.mozilla.org/en-US/");
    // cy.visit("https://benjamminj.github.io/testing-react-apps-talk/");

    // cy.contains("View the Demo").click();
    // cy.contains("Topic").click();
    // cy.focused().type("Cypress");
    // cy.contains("Search!").click();

    // cy.contains("cypress");
    // cy.contains(
    //   "Fast, easy and reliable testing for anything that runs in a browser."
    // )
    //   .siblings()
    //   .contains("See More")
    //   .click();

    // cy.contains("MIT License");
  });
});
